[package]
name = "event-service"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["full"] }
axum = { version = "0.8.9", features = ["default", "macros"]}
serde = { version = "1.0.228", features = ["derive"] }